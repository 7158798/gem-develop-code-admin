<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.manage.bus.dao.GTradeRecordOtcUsdtDAO">

	<!-- 实体和表的映射关系 -->
	<resultMap type="GTradeRecordOtcUsdt" id="responseMap">
		<result property="id" column="id"/>
		<result property="trade_type" column="trade_type"/>
		<result property="currency_id" column="currency_id"/>
		<result property="currency_symbol" column="currency_symbol"/>
		<result property="order_id" column="order_id"/>
		<result property="object_uid" column="object_uid"/>
		<result property="trade_time" column="trade_time"/>
		<result property="trade_price" column="trade_price"/>
		<result property="order_number" column="order_number"/>
		<result property="amount" column="amount"/>
		<result property="trade_status" column="trade_status"/>
		<result property="uid" column="uid"/>
		<result property="finance_id" column="finance_id"/>
		<result property="remark" column="remark"/>
		<result property="version" column="version"/>
		<result property="create_time" column="create_time"/>
		<result property="updated_time" column="updated_time"/>
	</resultMap>
	<!-- 获取分页数据 -->
	<select id="selectPage" parameterType="java.util.Map" resultMap="responseMap">
		SELECT * FROM g_trade_record_otc_usdt 
		WHERE 1=1 
		LIMIT
		<if test="PSIZE==null">10</if><if test="PSIZE!=null">#{PSIZE}</if>
		offset
		<if test="BEGIN==null">0</if><if test="BEGIN!=null">#{BEGIN}</if>
	</select>
	<!-- 获取分页数据总条数 -->
	<select id="selectPageCount" resultType="int" parameterType="java.util.Map">
		SELECT COUNT(1) FROM g_trade_record_otc_usdt
		WHERE 1=1 
	</select>
	<!-- 添加单条记录 -->
	<insert id="add" parameterType="GTradeRecordOtcUsdt" useGeneratedKeys="true" keyProperty="id">
		insert into g_trade_record_otc_usdt(trade_type,currency_id,currency_symbol,order_id,object_uid,trade_time,trade_price,order_number,amount,trade_status,uid,finance_id,remark,version,create_time,updated_time) values(#{trade_type},#{currency_id},#{currency_symbol},#{order_id},#{object_uid},#{trade_time},#{trade_price},#{order_number},#{amount},#{trade_status},#{uid},#{finance_id},#{remark},#{version},#{create_time},#{updated_time})
	</insert>
	<!-- 获取单条记录 -->
	<select id="getById" parameterType="long" resultMap="responseMap">
		SELECT * FROM g_trade_record_otc_usdt WHERE 
		id=#{id} 
	</select>
	<!-- 更新单条记录 -->
	<update id="update" parameterType="GTradeRecordOtcUsdt">
		update g_trade_record_otc_usdt
		<set>
				<if test="trade_type!=null">
					trade_type=#{trade_type},
				</if>
				<if test="currency_id!=null">
					currency_id=#{currency_id},
				</if>
				<if test="currency_symbol!=null">
					currency_symbol=#{currency_symbol},
				</if>
				<if test="order_id!=null">
					order_id=#{order_id},
				</if>
				<if test="object_uid!=null">
					object_uid=#{object_uid},
				</if>
				<if test="trade_time!=null">
					trade_time=#{trade_time},
				</if>
				<if test="trade_price!=null">
					trade_price=#{trade_price},
				</if>
				<if test="order_number!=null">
					order_number=#{order_number},
				</if>
				<if test="amount!=null">
					amount=#{amount},
				</if>
				<if test="trade_status!=null">
					trade_status=#{trade_status},
				</if>
				<if test="uid!=null">
					uid=#{uid},
				</if>
				<if test="finance_id!=null">
					finance_id=#{finance_id},
				</if>
				<if test="remark!=null">
					remark=#{remark},
				</if>
				<if test="version!=null">
					version=#{version},
				</if>
				<if test="create_time!=null">
					create_time=#{create_time},
				</if>
				<if test="updated_time!=null">
					updated_time=#{updated_time}
				</if>
		</set>
		where  
		id=#{id} 
	</update>
	<!-- 删除单条记录 -->
	<delete id="deleteById" parameterType="long">
		delete from g_trade_record_otc_usdt where 
		id=#{id} 
	</delete>
	<!-- 获取所有数据 -->
	<select id="selectAll" parameterType="java.util.Map" resultMap="responseMap">
		SELECT * FROM g_trade_record_otc_usdt where 1=1 
	</select>
</mapper>
